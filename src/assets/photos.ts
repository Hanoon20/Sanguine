import cover from './hero.webp';

/**
 * The cover photograph. One file: it is shown sharp for the moment the camera
 * arrives, then softened with a CSS blur rather than by swapping in a second
 * image, so there is nothing to load at the exact moment of the reveal.
 */
export const photos = { cover };
